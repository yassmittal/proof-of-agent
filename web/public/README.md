# public

Static files served as-is at the site root.

Right now it's just the default icons that came with `create-next-app` (`next.svg`, `vercel.svg`,
and a few others). The verifier doesn't actually use them — they're harmless leftovers. Drop a
logo or favicon here if you want one.
